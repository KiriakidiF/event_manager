<h1>New Invite</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.invite_path(@conn, :create, %{event_id: @event_id})) %>

<span><%= link "Back", to: Routes.invite_path(@conn, :index, %{event_id: @event_id}) %></span>
